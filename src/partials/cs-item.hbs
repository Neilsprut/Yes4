<div class="row">
  <div class="col-md-7 bg">
    <img src="{{image}}" alt="">
  </div>
  <div class="col-md-4 item">
    <h2>{{title}}</h2>
    <p>{{description}}</p>
    <a href="{{link}}" class="btn btn-acerta" target="_blank">{{btn-text}}</a>
  </div>
</div>
